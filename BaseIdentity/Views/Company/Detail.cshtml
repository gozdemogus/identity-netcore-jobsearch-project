@model Company


@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<section class="profile-detail">
    <div class="container">
        <div class="col-md-12">
            <div class="row">
                <div class="basic-information">
                    <div class="col-md-3 col-sm-3">
                        <img src="@Model.Logo" alt="" class="img-responsive">
                    </div>
                    <div class="col-md-9 col-sm-9">
                        <div class="profile-content">
                            <h2>@Model.CompanyName <h3><p></p></h3><span>@Model.CompanyName</span></h2>
                            @{
                                var url = Model.Website;
                            }
                            <ul class="information">
                                <li><span>Established:</span>@Model.From</li>
                                <li><span>Sector:</span>@Model.Sector</li>
                                <li><span>Website:</span><a href="https://@url" style="color: blue; font-weight: bold;">@url</a></li>
                                <li><span>Employee Quantity:</span>@Model.EmployeeQuantity</li>
                            </ul>
                            <br />
                        </div>
                    </div>
                    <ul class="social">
                        @if (Model.Facebook != null)
                        {
                            <li><a href="@Model.Facebook" class="facebook"><i class="fa fa-facebook"></i>Facebook</a></li>
                        }
                        @if (Model.Google != null)
                        {
                            <li><a href="@Model.Google" class="google"><i class="fa fa-google-plus"></i>Google Plus</a></li>
                        }
                        @if (Model.Twitter != null)
                        {
                            <li><a href="@Model.Twitter" class="twitter"><i class="fa fa-twitter"></i>Twitter</a></li>
                        }
                        @if (Model.Linkedin != null)
                        {
                            <li><a href="@Model.Linkedin" class="linkedin"><i class="fa fa-linkedin"></i>Linkedin</a></li>
                        }
                        @if (Model.Instagram != null)
                        {
                            <li><a href="@Model.Instagram" class="instagram"><i class="fa fa-instagram"></i>Instagram</a></li>
                        }
                    </ul>

                    <div class="panel panel-default">
                        <div class="panel-heading">
                            <i class="fa fa-user fa-fw"></i> About
                        </div>

                        <div class="panel-body">
                            <p>@Model.Explanation</p>
                        </div>
                    </div>

                    @*<div class="panel panel-default">
                        <div class="panel-heading">
                            <i class="fa fa-leaf fa-fw"></i> Minimum Technical Skills and Qualifications
                        </div>
                        <div class="panel-body">
                            <p></p>
                            <ul>
                                <li>@Model</li>
                            </ul>
                        </div>
                    </div>

                    <div class="panel panel-default">
                        <div class="panel-heading">
                            <i class="fa fa-cog fa-fw"></i> Additional Rights
                        </div>

                        <div class="panel-body">
                            <p></p>
                            <span class="service-tag">@Model.AdditionalRights</span>
                        </div>
                    </div>*@

                </div>
            </div>
        </div>
    </div>
</section>